version: '3.8'
services:
  backend:
  build: ./backend
  environment:
    - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
  ports:
    - '3001:3001'
  frontend:
    build: ./frontend
    ports:
    - '3000:5173'
  depends_on:
  - backend